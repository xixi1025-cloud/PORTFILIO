import { awards } from '../src/data/awards'; import { Container } from './Container'; import { Reveal } from './Reveal'; import { SectionHeader } from './SectionHeader';
export function Awards() { return <section id="awards" className="section section-surface awards-section"><Container><SectionHeader number="04" label="RECOGNITION" title="Recognition, not decoration." description="一些值得记录的结果。" /><div className="awards-list">{awards.map((item, i) => <Reveal key={`${item.year}-${item.name}-${i}`}><article className="award-item"><time>{item.year}</time><div><h3>{item.name}</h3>{item.work && <p>{item.work}</p>}</div><strong>{item.award}</strong></article></Reveal>)}</div></Container></section>; }

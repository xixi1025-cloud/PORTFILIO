import { projects } from '../src/data/projects'; import { ProjectCard } from './ProjectCard'; import { Reveal } from './Reveal';
type Category = { id:string; number:string; title:string; chinese:string; eyebrow:string; description:string };
export function PortfolioCategory({ category }: { category: Category }) { const items = projects.filter(p => p.category === category.id).sort((a,b) => a.order - b.order); return <section className="portfolio-category" aria-labelledby={`category-${category.id}`}><Reveal className="category-header"><p>{category.number}</p><div><span>{category.eyebrow}</span><h3 id={`category-${category.id}`}>{category.title}<small>{category.chinese}</small></h3>{category.description && <p>{category.description}</p>}</div></Reveal><div className="project-grid">{items.map((project, i) => <Reveal key={project.id} delay={(i % 2) * .08}><ProjectCard project={project} index={i} /></Reveal>)}</div></section>; }
